---
import ArticleFilters from './news/ArticleFilters.astro';

const { categories = [], programs = [], years = [], authors = [] } = Astro.props;
---

<ArticleFilters categories={categories} programs={programs} years={years} authors={authors} />
